OPENAI_API_KEY= KEY
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Required only for image generation and image modification workflows.
PIXELLAB_API_KEY= KEY

# Optional override if the Godot project root is not the repository root.
# GODOT_PROJECT_DIR=/absolute/path/to/your/godot/project
